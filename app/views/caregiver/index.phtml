<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Caregiver Dashboard - Maxmila Homecare</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons for icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS for Pink Theme -->
    <style>
        body {
            background-color: #f8e1f4; /* Light pink background */
        }
        .container {
            max-width: 800px;
            margin-top: 50px;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h2 {
            color: #d85b85; /* Pinkish color for header */
            font-size: 2.5em;
        }
        h3 {
            color: #c2185b;
            font-size: 1.8em;
        }
        .d-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
        }
        .btn {
            background-color: #e74c3c;
            border-color: #e74c3c;
            color: white;
            font-size: 1.2em;
            text-align: left;
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        .btn:hover {
            background-color: #c0392b;
            border-color: #c0392b;
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
        }
        .btn i {
            margin-right: 10px;
        }
        .welcome-section {
            text-align: center;
            margin-bottom: 30px;
        }
        .role-badge {
            display: inline-block;
            padding: 5px 15px;
            background-color: #e74c3c;
            color: white;
            border-radius: 20px;
            font-size: 0.9em;
            margin-top: 10px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="welcome-section">
        <h2>Welcome <?= htmlspecialchars($username ?? 'Caregiver') ?></h2>
        <div class="role-badge">Caregiver</div>
    </div>

    <h3 class="text-center mb-4">Dashboard Menu</h3>

    <div class="d-grid gap-3">
        <?php if (!empty($menuItems)): ?>
            <?php foreach ($menuItems as $item): ?>
                <a href="<?= $item['url'] ?>" class="btn btn-lg">
                    <i class="bi bi-<?= $item['icon'] ?>"></i>
                    <?= $item['text'] ?>
                </a>
            <?php endforeach; ?>
        <?php else: ?>
            <div class="alert alert-warning">
                No menu items available. Please contact administrator.
            </div>
        <?php endif; ?>
    </div>
</div>

<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>