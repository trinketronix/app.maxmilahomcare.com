<!DOCTYPE html>
<html>
<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS for Pink Theme -->
<head>
    <title>Patients List</title>
    <style>
        body {
            background-color: #f8e1f4; /* Light pink background */
        }
        .container {
            max-width: 600px;
            margin-top: 50px;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h2 {
            color: #d85b85; /* Pinkish color for header */
            font-size: 2.5em;
        }
        .form-label {
            color: #333;
        }
        .form-control {
            border-radius: 10px;
            box-shadow: none;
        }
        .btn-primary {
            background-color: #e74c3c;
            border-color: #e74c3c;
        }
        .btn-primary:hover {
            background-color: #c0392b;
            border-color: #c0392b;
        }
        .btn-link {
            color: #e74c3c; /* Red color for "Already have an account" link */
        }
        .btn-link:hover {
            color: #c0392b; /* Darker red on hover */
        }
        .d-grid {
            gap: 15px;
        }
        textarea.form-control {
            border-radius: 10px;
            box-shadow: none;
        }
        .form-control:focus {
            box-shadow: 0 0 5px rgba(231, 76, 60, 0.7); /* Red focus effect */
        }

        .list-group-item {
            background-color: #fce6f5; /* Light pink background for each list item */
            border: 1px solid #f8e1f4;
            border-radius: 10px;
            margin-bottom: 10px;
            padding: 15px;
        }
        .list-group-item:hover {
            background-color: #f8d1e6; /* Slightly darker pink for hover effect */
        }
        .form-control-static {
            font-size: 1.1em;
            color: #000;
        }
        .list-group-item h5 {
            color: #d85b85; /* Pinkish color for user names */
        }
        .list-group-item p {
            color: #000;
        }
        .list-group-item small {
            color: #777; /* Muted color for phone numbers */
        }

        .manager-row {
            display: flex;
            align-items: center;
            border: 1px solid red;
            padding: 10px;
            margin-bottom: 10px;
            background-color: #fce6f5; /* Light pink background for each list item */
            border-radius: 10px;
            
        }
        .manager-column {
            margin-right: 20px;
        }
        .register-btn {
            padding: 5px 10px;
            background-color: red;
            color: white;
            border: none;
            cursor: pointer;
        }
        .register-btn:hover {
            background-color: red;
        }
    </style>
</head>
<body>

<h2 class="text-center mb-4">Patients List</h2>
<center><a href="/main" class="btn btn-primary">Back to Menu</a></center>
     

    <div id="managers-list">
        <?php foreach ($managers as $manager): ?>
            <div class="manager-row">
                
                <div class="manager-column">
                    <button class="register-btn">
                        <i class="bi bi-geo-alt"></i> <!-- Direction icon -->
                    </button>
                </div>
                <div class="manager-column">
                    <button class="register-btn">
                        <i class="bi bi-telephone"></i> <!-- Call icon -->
                    </button>
                </div>
                <div class="manager-column">
                    <button class="register-btn">Check in</button>
                </div>
                <div class="manager-column">
                    Patient name: <?= $manager['lastname'].' '.$manager['firstname'] ?>
                </div>
            </div>
        <?php endforeach; ?>
    </div>

</body>
</html>
