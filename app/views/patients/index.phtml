<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $pageTitle ?? 'Patients List' ?></title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/css/theme.css" rel="stylesheet">
    <style>
        /* Page-specific styles */
        .manager-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            border: 1px solid #e74c3c;
            padding: 15px;
            margin-bottom: 15px;
            background-color: #fce6f5;
            border-radius: 10px;
            flex-wrap: wrap;
        }

        .manager-column {
            margin: 5px;
            display: flex;
            align-items: center;
        }

        .user-info {
            flex-grow: 1;
            margin: 0 10px;
            font-weight: bold;
            color: #333;
        }

        .action-buttons {
            display: flex;
            gap: 10px;
        }

        .patient-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
        }

        @media (max-width: 768px) {
            .manager-row {
                flex-direction: column;
                align-items: flex-start;
            }

            .user-info {
                margin: 10px 0;
                width: 100%;
            }

            .action-buttons {
                width: 100%;
                justify-content: space-between;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <h2 class="text-center mb-4"><?= $pageTitle ?? 'Patients List' ?></h2>

    <!-- Flash Messages -->
    <div class="flash-messages">
        <?= $this->flashSession->output() ?>
    </div>

    <!-- Back Button -->
    <div class="text-center mb-4">
        <button type="button" class="btn btn-danger" onclick="window.history.back();">
            <i class="bi bi-arrow-left-circle"></i> Back
        </button>
    </div>

    <!-- No Patients Message -->
    <?php if (empty($patients)): ?>
        <div class="alert alert-info text-center">
            No patients found.
        </div>
    <?php endif; ?>

    <!-- Patients List -->
    <div class="patient-container">
        <?php foreach ($patients as $index => $patient): ?>
            <div class="manager-row" data-patient-id="<?= $patient['id'] ?? '' ?>">
                <!-- Patient Info -->
                <div class="user-info">
                    <div><?= htmlspecialchars($patient['lastname'] ?? '') ?> <?= htmlspecialchars($patient['firstname'] ?? '') ?></div>
                    <div class="text-muted"><?= htmlspecialchars($patient['phone'] ?? '') ?></div>
                </div>

                <!-- Action Buttons -->
                <div class="action-buttons">
                    <!-- Location Button -->
                    <?php if (isset($patient['addresses'][0]['latitude']) && isset($patient['addresses'][0]['longitude'])): ?>
                        <button class="btn btn-sm btn-danger" onclick="viewLocation(<?= $patient['latitude'] ?>, <?= $patient['longitude'] ?>)">
                            <i class="bi bi-geo-alt"></i> Location
                        </button>
                    <?php endif; ?>

                    <!-- Call Button -->
                    <?php if (!empty($patient['phone'])): ?>
                        <a href="tel:<?= htmlspecialchars($patient['phone']) ?>" class="btn btn-sm btn-danger">
                            <i class="bi bi-telephone"></i> Call
                        </a>
                    <?php endif; ?>

                    <!-- Check-in Button -->
                    <a href="/patients/visit/<?= $patient['id'] ?? '' ?>" class="btn btn-sm btn-danger">
                        <i class="bi bi-calendar-check"></i> Check-in
                    </a>

                    <!-- View Details Button -->
                    <a href="/patients/view/<?= $patient['id'] ?? '' ?>" class="btn btn-sm btn-danger">
                        <i class="bi bi-eye"></i> Details
                    </a>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>

<!-- JavaScript -->
<script>
    // Function to open Google Maps with patient location
    function viewLocation(latitude, longitude) {
        if (latitude && longitude) {
            window.open(`https://www.google.com/maps?q=${latitude},${longitude}`, '_blank');
        } else {
            alert('Location information is not available for this patient.');
        }
    }

    // Enable all tooltips
    document.addEventListener('DOMContentLoaded', function() {
        // Initialize any components that need JavaScript here
    });
</script>
</body>
</html>