<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users List</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS for Pink Theme -->
    <style>
        body {
            background-color: #f8e1f4; /* Light pink background */
            font-family: 'Roboto', sans-serif; /* Default font */
            color: #333;
        }
        .container {
            max-width: 1200px;
            margin-top: 50px;
            padding: 30px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h2 {
            font-family: 'Poppins', sans-serif;
            color: #d85b85; /* Pinkish color for header */
            font-size: 2.5em;
            font-weight: 500;
            text-align: center;
        }
        .manager-row {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr 1fr; /* 4 columns */
            gap: 20px;
            background-color: #fce6f5; /* Light pink background for each row */
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 15px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .manager-row div {
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Roboto', sans-serif;
        }
        .manager-row a {
            padding: 10px 20px;
            background-color: red; /* All buttons will be red */
            color: white;
            text-align: center;
            border-radius: 5px;
            font-family: 'Poppins', sans-serif;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
        }
        .manager-row a:hover {
            background-color: darkred; /* Darker red on hover */
            transform: scale(1.05); /* Slight zoom effect on hover */
        }
        .manager-row .user-info {
            font-size: 1.1em;
            font-weight: bold;
            color: #d85b85;
        }
        .btn-danger:hover {
            background-color: darkred;
            color: white;
        }

    </style>
</head>
<body>
<center>
    <a href="/main" class="btn btn-danger" style="background-color: red; border: none; transition: background-color 0.3s;">
        <i class="bi bi-arrow-left-circle"></i> Back to Menu
    </a>
    <a href="/signup" class="btn btn-danger" style="background-color: red; border: none; transition: background-color 0.3s;">
        Create Account
    </a>
</center>
    <div class="container">
        <div id="managers-list" class="row">
            <?php foreach ($managers as $manager): ?>
                <div class="manager-row" data-id="<?= $manager['id'] ?>" data-username="<?= $manager['username'] ?>" >
                    <div class="user-info">
                        <?= $manager['username'].' '.$manager['lastname'].' '.$manager['firstname'] ?>
                    </div>
                    <div>
                        <a href="#" class="btn" onclick="setManagerIdAndRedirect(this)">Activate</a>
                    </div>
                    <div><a href="#" class="btn" onclick="setManagerIdAndUpdate(this)">Update</a>
                    </div>
                    <div><a href="#" class="btn" onclick="setManagerIdAndRole(this)">Role</a>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    </div>

    <!-- Move JavaScript to the bottom of the body to ensure elements are loaded before execution -->
    <script>
        function setManagerIdAndRedirect(button) {
            var row = button.closest('.manager-row');
            var managerId = row.getAttribute("data-id");
            console.log("Manager ID:", managerId);
            
            let date = new Date();
            date.setTime(date.getTime() + (1 * 24 * 60 * 60 * 1000)); // 1 day from now
            let expires = "expires=" + date.toUTCString();
            document.cookie = "activateid=" + managerId + "; " + expires + "; path=/";
            
            var username = row.getAttribute("data-username");
            document.cookie = "username=" + username + "; " + expires + "; path=/";
            
            window.location.href = "/activate"; 
        }

        function setManagerIdAndUpdate(button) {
            var row = button.closest('.manager-row');
            var managerId = row.getAttribute("data-id");
            console.log("Manager ID:", managerId);
            
            let date = new Date();
            date.setTime(date.getTime() + (1 * 24 * 60 * 60 * 1000)); // 1 day from now
            let expires = "expires=" + date.toUTCString();
            document.cookie = "activateid=" + managerId + "; " + expires + "; path=/";
            
            var username = row.getAttribute("data-username");
            document.cookie = "username=" + username + "; " + expires + "; path=/";
            
            window.location.href = "/userupdate"; 
        }

        function setManagerIdAndRole(button) {
            var row = button.closest('.manager-row');
            var managerId = row.getAttribute("data-id");
            console.log("Manager ID:", managerId);
            
            let date = new Date();
            date.setTime(date.getTime() + (1 * 24 * 60 * 60 * 1000)); // 1 day from now
            let expires = "expires=" + date.toUTCString();
            document.cookie = "activateid=" + managerId + "; " + expires + "; path=/";
            
            var username = row.getAttribute("data-username");
            document.cookie = "username=" + username + "; " + expires + "; path=/";
            
            window.location.href = "/changerole"; 
        }
    </script>
</body>
</html>
