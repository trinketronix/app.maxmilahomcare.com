<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Assign Patient</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS for Pink Theme -->
    <style>
        body {
            background-color: #f8e1f4;
        }
        .container {
            max-width: 800px;
            margin-top: 50px;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h2 {
            color: #d85b85;
            text-align: center;
            margin-bottom: 30px;
        }
        .profile-card {
            background-color: #fce6f5;
            border: 1px solid #f8e1f4;
            border-radius: 10px;
            margin-bottom: 20px;
            padding: 25px;
        }
        .profile-card:hover {
            background-color: #f8d1e6;
        }
        .profile-section {
            margin-bottom: 15px;
        }
        .profile-label {
            font-weight: bold;
            color: #d85b85;
            margin-bottom: 5px;
        }
        .profile-value {
            color: #333;
            font-size: 1.1em;
            margin-bottom: 15px;
        }
        .btn-primary {
            background-color: #e74c3c;
            border-color: #e74c3c;
            margin-top: 10px;
        }
        .btn-primary:hover {
            background-color: #c0392b;
            border-color: #c0392b;
        }
        .btn-danger {
            background-color: #e74c3c;
            border-color: #e74c3c;
        }
        .btn-danger:hover {
            background-color: darkred;
            color: white;
        }
        .flash-message {
            text-align: center;
            color: red;
            margin-bottom: 20px;
        }
        .actions-container {
            display: grid;
            grid-gap: 15px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
<div class="container">
    <h2>Confirm Assign Patient</h2>

    <!-- Flash Message Display -->
    <div class="flash-message">
        <?= $this->flashSession->output() ?>
    </div>

    <?php if (isset($userId) && isset($patientId)): ?>
        <div class="profile-card" data-id="<?= htmlspecialchars($userId) ?>">
            <div class="profile-section">
                <div class="profile-label">Caregiver</div>
                <div class="profile-value"><?= htmlspecialchars($username ?? 'Not provided') ?></div>
            </div>

            <div class="profile-section">
                <div class="profile-label">Patient</div>
                <div class="profile-value">
                    <?= htmlspecialchars($patientname ?? 'Unknown') ?> (ID: <?= htmlspecialchars($patientId ?? 'N/A') ?>)
                </div>
            </div>
        </div>

        <div class="actions-container">
            <!-- Back Button -->
            <button type="button" class="btn btn-danger" onclick="window.location.href = '/assignpatient';">
                Go to Assign Patient
            </button>

            <!-- Logout Button -->
            <button type="button" class="btn btn-primary" onclick="window.location.href='/logout'">
                <i class="bi bi-box-arrow-right"></i> Logout
            </button>
        </div>
    <?php else: ?>
        <div class="alert alert-danger">
            No profile data available. Please try again later or contact support.
        </div>
        <button type="button" class="btn btn-danger"
                onclick="window.location.href='/assignpatient';">
            <i class="bi bi-arrow-left-circle"></i> Back to Assign
        </button>
    <?php endif; ?>
</div>

<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
